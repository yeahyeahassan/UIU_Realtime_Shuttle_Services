// Initialize the map (This is a placeholder for real-time data)
function initMap() {
    var map = document.getElementById('map');
    map.innerHTML = '<p>Real-time shuttle location coming soon!</p>';
}

// Call map initialization
initMap();
